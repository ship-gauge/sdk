---
title: CI Gate
description: Eval + cost gate in pull requests
---

# CI Gate

Run `shipgauge gate check` in CI to compare pass rate, cost, and latency against a baseline. Use `--status-check` for required GitHub checks and `--pr-comment` for PR summaries.
